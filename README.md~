# go-itoa-
the library help transfer uint8 into []string, then can be transfer into string

var str string
strl := itoa.B2S(h.Sum(nil))
for i, _ := range strl {
    str += strl[i]
}

Then you can get string you want!
